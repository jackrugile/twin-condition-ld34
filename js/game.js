$.game = playground({
	width: 900,
	height: 600,
	scale: 1,
	smoothing: false
});

$.game.create = function() {
};

$.game.ready = function() {
};

$.game.step = function( dt ) {
};

$.game.mousedown = function( e ) {
	// game wide mousedown
};

$.game.keydown = function( e ) {
	// game wide keydown
};